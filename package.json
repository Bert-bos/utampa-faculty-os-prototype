{
  "name": "utampa-faculty-os-prototype",
  "version": "1.1.0",
  "private": true,
  "description": "Google-authenticated delivery shell for the private UTampa Faculty OS prototype.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}
